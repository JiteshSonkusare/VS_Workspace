using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;

namespace HackerRankSolution.StackAndQueues
{
    class QueuesTaleofTwoStacks
    {
        //static void Main(String[] args)
        //{
        //    Stack<int> push = new Stack<int>();
        //    Stack<int> pop = new Stack<int>();
        //    String count = Console.ReadLine();
        //    for (int i = 0; i < Convert.ToInt32(count); i++)
        //    {
        //        string[] line = Console.ReadLine().Split(' ');
        //        if (line[0] == "1")
        //        {
        //            push.Push(Convert.ToInt32(line[1]));
        //        }
        //        if (line[0] == "2")
        //        {
        //            if (!pop.Any())
        //                while (push.Count() > 0)
        //                    pop.Push(push.Pop());
        //            pop.Pop();
        //        }
        //        if (line[0] == "3")
        //        {
        //            if (!pop.Any())
        //                while (push.Count() > 0)
        //                    pop.Push(push.Pop());
        //            Console.WriteLine(pop.Peek());
        //        }
        //    }
        //}
    }
}
